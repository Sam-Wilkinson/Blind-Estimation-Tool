  <%= form_with(model: @room, local: true) do |f| %>
    <%= f.label :name, class: "form-label" %>
    <%= f.text_field :name, class: "form-control" %>
    <%= f.submit t('views.rooms.index.buttons.create'), class: "btn btn-warning" %>
  <% end %>